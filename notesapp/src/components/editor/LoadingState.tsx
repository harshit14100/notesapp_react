const LoadingNoteState = () => {
  return (
    <div className="w-full h-full bg-bg-right flex items-center justify-center">
      <p className="text-text-muted">Loading note...</p>
    </div>
  );
};

export default LoadingNoteState;